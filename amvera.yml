meta:
  environment: python
  toolchain:
    name: pip
    version: 3.13
build:
  requirementsPath: requirements.txt
run:
  command: fastapi run main.py
  persistenceMount: /data
  containerPort: 8000
